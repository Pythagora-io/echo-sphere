<form action="/search" method="GET" class="flex flex-col space-y-4">
  <div class="flex flex-col">
    <label for="searchTerm" class="mb-2 font-bold">Search Term:</label>
    <input type="text" id="searchTerm" name="term" placeholder="Enter search term..." class="border-2 border-gray-300 p-2 rounded-md" required>
  </div>
  <div class="flex flex-col">
    <label for="timeFilter" class="mb-2 font-bold">Time:</label>
    <select id="timeFilter" name="time" class="border-2 border-gray-300 p-2 rounded-md">
      <option value="all">All Time</option>
      <option value="day">Past 24 Hours</option>
      <option value="week">Past Week</option>
      <option value="month">Past Month</option>
      <option value="year">Past Year</option>
    </select>
  </div>
  <div class="flex flex-col">
    <label class="mb-2 font-bold">Sort By:</label>
    <div class="flex items-center space-x-4">
      <div>
        <input type="radio" id="sortNew" name="sort" value="new" checked>
        <label for="sortNew">New</label>
      </div>
      <div>
        <input type="radio" id="sortTop" name="sort" value="top">
        <label for="sortTop">Top</label>
      </div>
    </div>
  </div>
  <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Search</button>
</form>
<script>
  document.querySelector('form').addEventListener('submit', function(event) {
    console.log('Search form submitted with term:', document.getElementById('searchTerm').value);
  });
</script>