<!DOCTYPE html>
<html lang="en" class="<%= theme === 'dark' ? 'dark' : '' %>">
<%- include('partials/_head.ejs') %>
  <body class="bg-gray-100 dark:bg-gray-800 text-gray-900 dark:text-white">
<%- include('partials/_header.ejs') %>
    <main class="container mx-auto mt-5">
      <h2 class="text-2xl font-bold text-gray-900 dark:text-gray-200">Your Profile</h2>
      <% if (success) { %>
        <div class="bg-green-100 border border-green-400 text-green-700 dark:bg-green-800 dark:border-green-900 dark:text-green-200 px-4 py-3 rounded relative" role="alert">
          Profile updated successfully!
        </div>
      <% } %>
      <form action="/profile" method="POST" enctype="multipart/form-data" class="mt-4">
        <div class="mb-4">
          <label for="avatar" class="block text-gray-700 dark:text-gray-300 text-sm font-bold mb-2">Avatar</label>
          <% if (user.avatar) { %>
            <img src="<%= user.avatar %>" alt="Your Avatar" class="w-40 h-40 object-cover rounded-full">
          <% } %>
          <input type="file" name="avatar" class="form-input mt-1 block w-full border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-300 rounded-md shadow-sm">
        </div>
        <div class="mb-4">
          <label for="bio" class="block text-gray-700 dark:text-gray-300 text-sm font-bold mb-2">Bio</label>
          <textarea name="bio" placeholder="Tell us about yourself" required class="form-textarea mt-1 block w-full border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-300 rounded-md shadow-sm"><%= user.bio %></textarea>
        </div>
        <!-- Add more fields for settings as needed -->
        <div class="mb-4">
          <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline dark:bg-blue-600 dark:hover:bg-blue-800">Update Profile</button>
        </div>
      </form>
      <div class="theme-settings mt-8">
        <h3 class="text-xl font-bold mb-4 text-gray-900 dark:text-gray-200">Theme Settings</h3>
        <button id="themeToggle" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline dark:bg-blue-600 dark:hover:bg-blue-800">Change Theme</button>
      </div>
    </main>
  </body>
<%- include('partials/_footer.ejs') %>
<script src="/js/themeToggleHandler.js"></script>
</html>